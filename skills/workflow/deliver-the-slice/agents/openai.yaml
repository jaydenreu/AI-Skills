interface:
  display_name: "Deliver The Slice"
  short_description: "Execute one routed slice with evidence"
  default_prompt: "Use $deliver-the-slice to execute one routed issue, honor its $model-route assignments, validate it, and record the routing outcome."

policy:
  allow_implicit_invocation: false
