interface:
  display_name: "Set The Rails"
  short_description: "Confirm project rails with the user"
  default_prompt: "Use $set-the-rails to confirm this repo's Rails Contract, including its Work Path, AI routing, thread policy, review, and done rules."

policy:
  allow_implicit_invocation: false
