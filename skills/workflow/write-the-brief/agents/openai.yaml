interface:
  display_name: "Write The PRD"
  short_description: "Publish the canonical delivery PRD"
  default_prompt: "Use $write-the-brief to publish the accepted decisions as this project's canonical PRD."

policy:
  allow_implicit_invocation: false
