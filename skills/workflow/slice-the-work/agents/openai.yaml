interface:
  display_name: "Slice The Work"
  short_description: "Create routed, agent-ready delivery issues"
  default_prompt: "Use $slice-the-work to create vertical issues, name justified exceptions, assign dependency waves, and route every accepted issue and container with $model-route."

policy:
  allow_implicit_invocation: false
