interface:
  display_name: "Model Route"
  short_description: "Route models, agents, cost, and review"
  default_prompt: "Use $model-route to print the lowest-cost credible task-intensity route, naming the agent type, model, and reasoning level for the root and every worker."

policy:
  allow_implicit_invocation: false
